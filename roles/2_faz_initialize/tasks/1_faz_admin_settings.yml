- name: 'FAZ Admin settings'
  fortinet.fortianalyzer.faz_cli_system_admin_setting:
    access_token: "{{ ansible_facts.access_token }}"
    cli_system_admin_setting:
      gui_theme: "{{ gui_theme }}"
      idle_timeout: "{{ idle_timeout }}"
      idle_timeout_gui: "{{ idle_timeout_gui }}"
